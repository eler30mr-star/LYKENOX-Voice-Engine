# LYKENOX residual-codebook oracle execution evidence

**Policy:** LYX-POL-001  
**Status:** oracle generated; awaiting human held-out listening  
**Source of execution result:** owner-reported local CPU run on 2026-09-01  
**Product acceptance:** NOT granted

## Purpose

This record preserves the first completed CELP-style residual-codebook capacity run after rejection of parametric synthetic excitation. It does not authorize a selector, model training, checkpoint creation, or production integration.

The codebook path remains diagnostic. The held-out residual is allowed only as an oracle search target and is not allowed to enter the codebook.

## Reported local execution result

The owner reported that `scripts/diagnostic_residual_codebook_oracle_v1.py` completed successfully with:

```json
{
  "status": "ready_for_residual_codebook_oracle_listening",
  "policy_id": "LYX-POL-001",
  "device": "cpu",
  "codebook_build_status": "built_from_owned_train_real_residual",
  "retained_codeword_count": 6234,
  "bucket_count": 58,
  "heldout_item_count": 3,
  "training_executed": false,
  "optimizer_created": false,
  "checkpoint_written": false,
  "selector_training_authorized_by_this_run": false,
  "next_action": "listen_to_codebook_oracle_vs_reference_and_real_residual_ceiling_before_training_any_selector"
}
```

Reported local artifacts:

- `models/lykenox_identity/calibration/residual_codebook_v1.pt`
- `models/lykenox_identity/evaluation/vocoder_minimum_phase_residual_codebook_oracle_v1/`
- held-out `__residual_codebook_oracle.wav`
- held-out `__selected_codebook_residual.wav`
- held-out `__reference.wav`
- the oracle report generated by the diagnostic

## What this run establishes

1. The owned-train residual codebook builder completed on CPU.
2. The resulting codebook retained 6,234 codewords across 58 buckets.
3. Three held-out utterances were rendered through oracle analysis-by-synthesis search.
4. No selector was trained.
5. No optimizer was created.
6. No model checkpoint was written.
7. The run itself did not authorize selector training.

## What this run does NOT establish

This execution does not establish that the codebook is perceptually sufficient. It does not establish that a product-time selector can choose the same codewords or gains without access to the held-out target residual. It does not authorize production integration or any new persistent training.

Under LYX-POL-001, metrics and successful execution can reject candidates but cannot accept voice quality. The next authority is complete held-out listening.

## Required listening comparison

For all three held-out utterances, compare:

1. `__residual_codebook_oracle.wav` against `__reference.wav`;
2. `__residual_codebook_oracle.wav` against the clean real-residual resynthesis from Step 3f;
3. optionally `__selected_codebook_residual.wav` against the extracted real residual to diagnose source-detail mismatch.

The relevant perceptual question is whether the CELP-style oracle removes the previously persistent gangoso/rough/radio-interference character and approaches the clean real-residual ceiling.

## Gate state

`RESIDUAL_SELECTOR_TRAINING_CURRENTLY_AUTHORIZED = False`

No selector or gain predictor may be trained from this execution result alone. Human listening of all three complete held-out utterances is required first.
